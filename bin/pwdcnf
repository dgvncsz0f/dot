#!/bin/bash

pwsroot="$HOME/pwsstore"
binroot=$HOME/.dot/bin

pws_path()
{
  local file=$(/usr/bin/basename $1)
  pws_path="${pwsroot}/${file}.gpg"
}

_pwdcomp()
{
  local cur="${COMP_WORDS[COMP_CWORD]}"
  COMPREPLY=( $(compgen -o filenames -W "$(pwdls | xargs echo)" -- "${cur}" ) )
  return 0
}

