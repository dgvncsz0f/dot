set folder      = "imaps://hostname/INBOX"
set spoolfile   = "+."
set signature   = "~/.esig"
set from        = "user@isp.com"
set envelope_from_address = "user@isp.com"
set smtp_url = smtp://user@isp.com"

source "/etc/Muttrc"
source /usr/share/doc/mutt/examples/gpg.rc
source ~/.mutt/mutt-vars
source ~/.mutt/mutt-aliases
source ~/.mutt/mutt-color
source ~/.mutt/mutt-bindings
source ~/.mutt/mutt-views
